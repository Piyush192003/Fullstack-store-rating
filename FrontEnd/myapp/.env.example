# API base URL used by the frontend at build time (no trailing slash).
# After the first deploy, set this in Vercel Project Settings -> Environment
# Variables to your Heroku API root, e.g.:
VITE_API_URL=/api-placeholder-set-in-vercel-dashboard
